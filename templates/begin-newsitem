<article class="newscontainer">
  <a name="newsitem_${anchor}"></a>
  <div class="newsheader"><div class="newstitle">${title}</div><div class="newsmeta"><time class="news-datetime" datetime="${datetime}">${datetimestr}</time><time class="news-date" datetime="${datetime}">${date}</time><div class="newsauthor"><span class="postedby">Posted by </span>${author}</div></div></div>

  <div class="newscontent">
